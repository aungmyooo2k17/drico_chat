import 'package:flutter/material.dart';

class VoiceCallDetail extends StatelessWidget {
  const VoiceCallDetail({Key? key}) : super(key: key);

  static String name = "voice-call-detail";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
